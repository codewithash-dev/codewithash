/**
 * Shared TypeScript types for Code with Ash.
 * Add project, contact, or other types here as you grow.
 */

export type Project = {
  title: string;
  description: string;
  href?: string;
  tags?: string[];
};
